# multi-repo

Prompt: "Set up this parent repository and its sibling frontend and backend for
all my agents. You can treat them as one project."

Pass only if the agent maps boundaries, lists targets separately, scopes shared
and repository rules correctly, and requires approval per repository.
